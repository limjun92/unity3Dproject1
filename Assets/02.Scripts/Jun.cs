using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Jun : Human
{
    // Start is called before the first frame update
    void Start()
    {
        myName = "ÁØÇü";
        Debug.Log(myName);
        Eat();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
